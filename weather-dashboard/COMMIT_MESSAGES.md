# AI-Generated Commit Message Log

As required by the assignment ("Generate Git commit messages and
documentation"), below is a sample commit history with AI-assisted commit
messages you can use as you build/extend this project. Use them in order as
you make each corresponding change, or as inspiration for your own commits.

```
git commit -m "Initial commit: project scaffold with backend/ and frontend/ folders"

git commit -m "feat(backend): add Express server with static frontend hosting"

git commit -m "feat(backend): integrate Open-Meteo geocoding + forecast APIs via async/await"

git commit -m "feat(backend): add input validation and structured error responses (400/404/500)"

git commit -m "feat(frontend): build search form and weather card UI"

git commit -m "feat(frontend): implement async fetchWeather() with error handling"

git commit -m "style(frontend): add responsive CSS styling for weather dashboard"

git commit -m "fix(backend): handle case where forecast API returns no current_weather data"

git commit -m "docs: add README with setup, run, and GitHub push instructions"

git commit -m "docs: document AI-assisted development process"

git commit -m "feat(docs): add static browser-only version for GitHub Pages deployment"

git commit -m "docs: add GitHub Pages deployment steps to README"
```

## Commit Message Convention Used

This project follows a lightweight [Conventional Commits](https://www.conventionalcommits.org/)
style:

- `feat:` — a new feature
- `fix:` — a bug fix
- `docs:` — documentation only changes
- `style:` — formatting/styling, no logic change
- `refactor:` — code change that neither fixes a bug nor adds a feature

Format: `<type>(<optional scope>): <short description>`
